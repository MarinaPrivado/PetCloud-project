# OpenAI API Configuration
OPENAI_API_KEY=sua-chave-api-aqui

# Como obter sua chave API OpenAI:
# 1. Acesse: https://platform.openai.com/api-keys
# 2. Faça login ou crie uma conta
# 3. Clique em "Create new secret key"
# 4. Copie a chave gerada
# 5. Renomeie este arquivo para .env (remova o .example)
# 6. Cole sua chave após o = acima

# IMPORTANTE: 
# - Nunca compartilhe sua chave API publicamente
# - Adicione .env ao .gitignore para não commitar a chave
# - O arquivo .env já está no .gitignore por padrão
